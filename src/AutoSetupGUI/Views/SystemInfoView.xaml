<Page x:Class="AutoSetupGUI.Views.SystemInfoView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="System Information"
      Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Page Header -->
            <DockPanel Margin="0,0,0,20">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button x:Name="BtnCopy" Content="Copy to Clipboard" Style="{DynamicResource SecondaryButtonStyle}"
                            Click="BtnCopy_Click" Margin="0,0,8,0"/>
                    <Button x:Name="BtnRefresh" Content="Refresh" Style="{DynamicResource SecondaryButtonStyle}"
                            Click="BtnRefresh_Click"/>
                </StackPanel>
                <TextBlock Text="System Information" Style="{DynamicResource HeaderTextStyle}" VerticalAlignment="Center"/>
            </DockPanel>

            <!-- Asset Information -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Asset Information" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Service Tag:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtServiceTag" Text="--" Margin="0,0,20,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Computer Name:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="TxtComputerName" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Manufacturer:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtManufacturer" Text="--" Margin="0,0,20,0" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Model:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="TxtModel" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- BIOS Information -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="BIOS" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Version:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Column="1" x:Name="TxtBiosVersion" Text="--" Margin="0,0,20,0" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Column="2" Text="Release Date:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Column="3" x:Name="TxtBiosDate" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Operating System -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Operating System" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" x:Name="TxtOSName" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtOSVersion" Text="--" Margin="0,0,20,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Build:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="TxtOSBuild" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Architecture:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtOSArch" Text="--" Margin="0,0,20,0" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="2" Grid.Column="2" Text="Last Boot:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="2" Grid.Column="3" x:Name="TxtLastBoot" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Network Information -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Network" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ethernet MAC:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtEthernetMac" Text="--" Margin="0,0,20,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="WiFi MAC:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="TxtWifiMac" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Domain:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtDomain" Text="--" Margin="0,0,20,0" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Domain Joined:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="TxtDomainJoined" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Hardware -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Hardware" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Processor:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtProcessor" Text="--" Margin="0,0,0,12" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Cores:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtCores" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Total RAM:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" x:Name="TxtRAM" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Storage -->
            <Border Style="{DynamicResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Storage" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <ItemsControl x:Name="DiskList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DriveLetter}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <ProgressBar Grid.Column="1" Value="{Binding UsedPercentage, Mode=OneWay}" Minimum="0" Maximum="100"
                                                 Height="16" Margin="0,0,12,0" Style="{DynamicResource ModernProgressBarStyle}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding FreeSpaceFormatted}" HorizontalAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Grid.Column="3" Text="{Binding TotalSizeFormatted}" HorizontalAlignment="Right" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- SCCM Client -->
            <Border Style="{DynamicResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="SCCM Client" Style="{DynamicResource SubheaderTextStyle}" Foreground="{DynamicResource UniversityGoldBrush}" Margin="0,0,0,16"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Installed:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="TxtSCCMInstalled" Text="--" Margin="0,0,20,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Version:" FontWeight="SemiBold" Margin="0,0,0,12" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="0" Grid.Column="3" x:Name="TxtSCCMVersion" Text="--" Margin="0,0,0,12" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Site Code:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" x:Name="TxtSCCMSiteCode" Text="--" Margin="0,0,20,0" Foreground="{DynamicResource TextSecondaryBrush}"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Management Point:" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Grid.Row="1" Grid.Column="3" x:Name="TxtSCCMMP" Text="--" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
