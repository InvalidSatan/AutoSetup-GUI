<Page x:Class="AutoSetupGUI.Views.LogViewerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Log Viewer"
      Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button x:Name="BtnOpenFolder" Content="Open Folder" Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnOpenFolder_Click" Margin="0,0,8,0"/>
                <Button x:Name="BtnClear" Content="Clear" Style="{StaticResource SecondaryButtonStyle}"
                        Click="BtnClear_Click"/>
            </StackPanel>
            <TextBlock Text="Log Viewer" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <CheckBox x:Name="ChkDebug" Content="Debug" IsChecked="False" Margin="0,0,16,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                <CheckBox x:Name="ChkInfo" Content="Info" IsChecked="True" Margin="0,0,16,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                <CheckBox x:Name="ChkWarning" Content="Warning" IsChecked="True" Margin="0,0,16,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                <CheckBox x:Name="ChkError" Content="Error" IsChecked="True" Margin="0,0,16,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                <CheckBox x:Name="ChkSuccess" Content="Success" IsChecked="True" Checked="Filter_Changed" Unchecked="Filter_Changed"/>

                <Separator Margin="16,0"/>

                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="TxtSearch" Width="200" TextChanged="TxtSearch_TextChanged" VerticalContentAlignment="Center" Padding="8,4"/>
            </StackPanel>
        </Border>

        <!-- Log Content -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <ListBox x:Name="LogList"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="8,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding FormattedTimestamp}"
                                       FontFamily="Consolas" FontSize="12"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>

                            <Border Grid.Column="1" CornerRadius="3" Padding="6,2" Margin="4,0"
                                    HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource PendingBackgroundBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Level}" Value="Success">
                                                <Setter Property="Background" Value="{StaticResource SuccessBackgroundBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                <Setter Property="Background" Value="{StaticResource WarningBackgroundBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                                <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Level}" Value="Info">
                                                <Setter Property="Background" Value="#E8F4FC"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Level}" FontSize="10" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Success">
                                                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Info">
                                                    <Setter Property="Foreground" Value="{StaticResource InfoBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>

                            <TextBlock Grid.Column="2" Text="{Binding Message}"
                                       FontFamily="Consolas" FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#F8F8F8" Padding="12,8" Margin="0,16,0,0" CornerRadius="4">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" x:Name="TxtLogPath" Text="" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock x:Name="TxtLogCount" Text="0 entries" Foreground="{StaticResource TextSecondaryBrush}"/>
            </DockPanel>
        </Border>
    </Grid>
</Page>
